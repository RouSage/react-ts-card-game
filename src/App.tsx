import React from 'react';
import GameBoard from './components/GameBoard';

const App = (): JSX.Element => {
  return (
    <div className='App'>
      <GameBoard />
    </div>
  );
};

export default App;
